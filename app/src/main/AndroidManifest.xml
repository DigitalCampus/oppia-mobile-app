<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:name=".application.App"
        tools:replace="android:allowBackup"
        android:icon="@mipmap/dc_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dc_logo"
        android:supportsRtl="true"
        android:theme="@style/Oppia.Theme">
        <activity
            android:name="org.digitalcampus.oppiamobile.ui.auth.AuthActivity"
            android:exported="true"
            android:theme="@style/Oppia.ToolbarTheme"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.digitalcampus.oppiamobile.ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Oppia.ToolbarTheme"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
        </activity>
    </application>

</manifest>
